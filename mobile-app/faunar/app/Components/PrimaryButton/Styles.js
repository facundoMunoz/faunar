import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  saveAnimalButton: {
    height: 55,
    width: '70%',
    backgroundColor: 'black',
    borderRadius: 25,
    color: 'white',
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 25,
  },
  saveAnimalButtonText: {
    color: 'white',
    fontSize: 15,
    fontWeight: 'bold',
  },
});

export default styles;
